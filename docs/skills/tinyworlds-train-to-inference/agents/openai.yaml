interface:
  display_name: "TinyWorlds Train to Inference"
  short_description: "Closed-loop training and inference gate validator"
  default_prompt: "Use $tinyworlds-train-to-inference to run gated training, inference validation, and automatic dynamics retraining when inference fails."
