interface:
  display_name: "TinyWorlds Train to Inference"
  short_description: "闭环训练推理 + 双卡进程守卫"
  default_prompt: "Use $tinyworlds-train-to-inference to stop stale/extra training processes, enforce dual-GPU monitored training, run git evidence audit, execute full gated training, validate inference, auto-retrain dynamics on failure, and generate W&B/API-first analysis reports."
