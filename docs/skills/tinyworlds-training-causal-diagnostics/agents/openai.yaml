interface:
  display_name: "TinyWorlds Auto Training Loop"
  short_description: "自动门禁训练 + 双卡/进程守卫"
  default_prompt: "Use $tinyworlds-training-causal-diagnostics to run gate-based TinyWorlds training, auto-kill extra training processes, enforce dual-GPU runtime checks, and retune/restart until all stages pass."
